# OS
.DS_Store
Thumbs.db

# Editors
.vscode/
.idea/
*.swp

# Env / secrets
.env
.env.*

# Node (if this project ever grows one)
node_modules/

# Claude Code runtime state (not the launch.json config, just ephemeral session files)
.claude/scheduled_tasks.lock
